// string literals with built-ins like len, cstr, assert, and formatted assert
fn main() {
	s := "hello\n"
	var l int = len(s)

	assert l > 0
	assert l > 1, "len=%d", l

	_ = cstr(s)
	assert l == 6
}
