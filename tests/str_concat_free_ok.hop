// Verifies string concatenation dealloc is accepted.
fn caller() {
	var r &str = "abc"
	var s *str = concat(r, r)
	assert len(s) == 6
	dealloc s

	var arr *[u8 4] = alloc [u8 4]
	dealloc arr
}

fn main() {
	caller()
}
