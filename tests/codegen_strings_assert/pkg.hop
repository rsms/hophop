// Supports codegen strings assert by providing the pkg fixture source.
pub fn Main(x i32) i32

fn Main(x i32) i32 {
	var a &str = "hello"
	var b &str = "hello"
	var n int  = len(a)

	_ = cstr(b)
	assert x >= 0
	assert x > 1, "x=%d", x
	assert n > 0

	return x
}
