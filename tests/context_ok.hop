// Verifies ambient context is accepted.
fn make_ptr() *i32 {
	return alloc i32
}

fn log_msg(msg &str) {
	print(msg)
}

fn caller() {
	var p *i32 = make_ptr()
	dealloc p

	{
		context.logger.prefix = "inner"
		log_msg("ok")
	}

	log_msg("done")
}

fn main() {
	caller()
}
