// Verifies generic function inference and evaluator equality through readonly slice views.
fn eq[T](a, b &[T]) bool {
	return *a == *b
}

fn main() {
	a := [1, 2, 3]
	b := [1, 2, 3]
	assert eq(a, b)
}
