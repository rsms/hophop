// Verifies explicit allocator syntax for alloc and dealloc.
fn main() {
	var ma     = context.allocator
	var a *i32 = alloc i32 in ma
	var b *i32 = alloc i32 in ma
	*a = 10
	*b = 32
	assert *a + *b == 42
	dealloc a, b in ma
}
