// Verifies optional alloc returns null when the ambient allocator fails.
import "testing" { FailingAllocator }

fn main() {
	ma := FailingAllocator{}
	context.allocator = ma
	var p ?*i32 = alloc i32
	assert p == null
}
