// Verifies evaluator failures include nested call traces.
import "testing" { FailingAllocator }

fn leaf() {
	alloc i32
}

fn middle() {
	leaf()
}

fn main() {
	context.allocator = FailingAllocator{}
	middle()
}
